
@inject UserManager<ApplicationUser> UserManager;
@if (!User.Identity!.IsAuthenticated)
{
   
    <ul class="navbar-nav">
        <li class="nav-item">
            <a class="nav-link" asp-controller="Account" asp-action="Login"><i class="bi bi-box-arrow-in-right"></i> Login</a>
        </li>
        <li class="nav-item">
            <a class="nav-link" asp-controller="Account" asp-action="Register"><i class="bi bi-person-plus"></i> Register</a>
        </li>
    </ul>
}
else
{
   
    <div class="dropdown">
        <button class="btn btn-outline-success dropdown-toggle" type="button" id="profile-btn" data-bs-toggle="dropdown" aria-expanded="false">
            <i class="bi bi-person-badge"></i>Hello @UserManager.GetUserName(User)
        </button>
        <ul class="dropdown-menu" aria-labelledby="profile-btn">
            <li>
                <a class="dropdown-item" asp-controller="Orders" asp-action="AllOrder">
                    <i class="bi bi-list"></i>Orders
                </a>
            </li>
            <li>
                <a class="dropdown-item" asp-action="Login" asp-controller="Account">Logout</a>
            </li>
        </ul>
    </div>
}

<!-- User details -->
@* @if (User.Identity.IsAuthenticated)
{
    <p>User is authenticated</p>
}
else
{
    <p>User is not authenticated</p>
} *@